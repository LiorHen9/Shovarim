# Firebase Functions Gen 2 env-file convention: bundled into the deploy for
# project "shovarim-prod" only (docs/DECISIONS.md #46). Not a secret — same
# value already public in apphosting.yaml — this just closes the gap that
# apphosting.yaml's NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET never reaches Cloud
# Functions, only the Next.js App Hosting backend.
STORAGE_BUCKET=shovarim-prod.firebasestorage.app
